# Copy into the exported dataset folder as data.yaml and adjust names to the
# actual label ID order. Never change names order without remapping label files.
path: .
train: train/images
val: valid/images
test: test/images
names:
  0: forklift
  1: person
